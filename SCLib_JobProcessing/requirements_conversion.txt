# Conversion Script Dependencies
# Required by convert_tif_to_idx_rgb.py and other conversion scripts

# Image processing
matplotlib>=3.7.0
rasterio>=1.3.0
tifffile>=2023.1.0
Pillow>=10.1.0  # Already in requirements_fastapi.txt, but listed here for clarity
large-image>=1.25.0
large-image-source-tifffile
large-image-source-zarr
large-image-source-pil
opencv-python-headless>=4.8.0
scikit-image>=0.21.0

# Scientific computing (may already be installed via other requirements)
numpy>=1.24.0
h5py>=3.9.0  # HDF5 file support
netCDF4>=1.6.4  # NetCDF file support

# Geospatial processing
# Note: gdal==3.8.4 is installed separately in Dockerfile before rasterio
pyproj>=3.6.0
shapely>=2.0.0

# AWS and cloud storage
boto3>=1.34.0
awscli>=1.32.0

# Visualization (if needed for conversion scripts)
vtk>=9.2.0
colorcet>=3.0.0

# Additional utilities
xmltodict>=0.13.0
pytz>=2023.3

